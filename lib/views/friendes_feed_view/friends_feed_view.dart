import 'package:flutter/material.dart';

class FriendsFeedView extends StatelessWidget {
  const FriendsFeedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Friends Feed'),
    );
  }
}
