import 'package:flutter/material.dart';

class InpoxView extends StatelessWidget {
  const InpoxView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inpox'),
    );
  }
}
